<view class="full warp">
  <swiper 
    autoplay="{{autoplay}}" 
    bindanimationfinish="bannerChange"
    >
    <block wx:for="{{data.magazine}}" wx:key>
      <swiper-item class='box'>
        <view class="banner-warp flex-col">
          <image src="{{item}}" mode="aspectFit" class="banner-image" />
        </view>
      </swiper-item>
    </block>
  </swiper>
</view>